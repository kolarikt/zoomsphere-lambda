#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../serverless/bin/serverless" "$@"
  ret=$?
else 
  node  "$basedir/../serverless/bin/serverless" "$@"
  ret=$?
fi
exit $ret
